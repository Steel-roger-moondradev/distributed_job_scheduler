{
  "name": "distributed-job-scheduler",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=database && npm run build --workspace=observability && npm run build --workspace=shared && npm run build --workspace=@scheduler/api && npm run build --workspace=scheduler && npm run build --workspace=worker",
    "dev": "npm run dev --workspaces",
    "lint": "npm run lint --workspaces",
    "test": "npm run test --workspaces"
  },
  "dependencies": {
    "bullmq": "^5.79.1",
    "dotenv": "^17.4.2",
    "ioredis": "5.10.1",
    "prom-client": "^15.1.3"
  },
  "devDependencies": {
    "@types/node": "^20.19.43",
    "typescript": "^5.9.3"
  }
}
